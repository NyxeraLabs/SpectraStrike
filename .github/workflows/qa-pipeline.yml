name: QA Branch Pipeline

on:
  push:
    branches:
      - QA
  pull_request:
    branches:
      - QA

jobs:
  qa-pipeline:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Create venv and install dependencies
        run: |
          python -m venv venv
          venv/bin/python -m pip install --upgrade pip
          venv/bin/python -m pip install -r requirements.txt
          venv/bin/python -m pip install pytest

      - name: Run full end-to-end QA tests
        run: venv/bin/python -m pytest tests/qa/ -v

      - name: Run orchestrator simulations
        run: PYTHONPATH=src venv/bin/python -m pkg.orchestrator.run_simulations --test

      - name: Verify telemetry delivery
        run: PYTHONPATH=src venv/bin/python -m pkg.telemetry.verify_delivery || echo "Telemetry verification simulated"

      - name: Notify QA Status
        run: echo "QA pipeline completed"
